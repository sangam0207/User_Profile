<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>USER-PROFILE</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      background-color: #3498db; /* New background color */
    }

    .profile-container {
      max-width: 800px;
      width: 90%;
      padding: 40px;
      background-color: #2c3e50;
      border-radius: 15px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
      color: white;
    }

    .profile-header {
      text-align: center;
      margin-bottom: 30px;
    }

    .profile-content {
      display: flex;
    }

    .profile-image {
      flex: 0 0 240px;
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-right: 40px;
    }

    .profile-image img {
      width: 240px;
      height: 240px;
      border-radius: 50%;
      transition: transform 0.3s;
    }

    .profile-image img:hover {
      transform: scale(1.1);
    }

    .profile-logos {
      display: flex;
      gap: 15px;
      margin-top: 20px;
    }

    .profile-logos a {
      display: block;
    }

    .profile-logos img {
      width: 60px; /* Larger logo size */
      height: 60px; /* Larger logo size */
      transition: transform 0.2s;
      filter: invert(1);
    }

    .profile-logos img:hover {
      transform: scale(1.2);
    }

    .profile-details {
      flex: 1;
    }

    .profile-details h2 {
      font-size: 28px;
      margin-bottom: 20px;
      font-weight: bold;
    }

    .profile-details p {
      margin: 8px 0;
      font-size: 18px;
    }

    .profile-details strong {
      color: #fc2811;
    }

    .submit-button {
      text-align: center;
      margin-top: 30px;
    }

    .submit-button button {
      padding: 12px 24px;
      background-color: #fd3c27;
      font-size: 18px;
      transition: background-color 0.3s;
    }

    .submit-button button:hover {
      background-color: #c0392b;
    }
  </style>
</head>
<body>
  <div class="profile-container">
    <div class="profile-header">
      <h1><- Hello Everyone -></h1>
    </div>
    <div class="profile-content">
      <div class="profile-image">
        <img src="{{user_img}}" alt="{{user_name}}">
        <div class="profile-logos">
          <a href="{{user_linkedin}}" target="_blank"><img src="/logo/linkedin.png" alt="LinkedIn"></a>
          <a href="{{user_github}}" target="_blank"><img src="/logo/github.png" alt="GitHub"></a>
          <a href="{{user_insta}}" target="_blank"><img src="/logo/insta.jpg" alt="insta"></a>
        </div>
      </div>
      <div class="profile-details">
        <h2><span id="profileName">I am {{user_name}}</span></h2>
        <p><strong>Age:</strong> <span id="profileAge">{{user_age}}</span> years old</p>
        <p><strong>Gender:</strong> <span id="profileGender">{{user_gender}}</span></p>
        <p><strong>Email:</strong> <span id="profileEmail">{{user_email}}</span></p>
        <p><strong>Contact:</strong> <span id="profileContact">{{user_phone}}</span></p>
        <p><strong>Address:</strong> <span id="profileAddress">{{user_address}}</span></p>
        <p><strong>College:</strong> <span id="profileCollege">{{user_college}}</span></p>
        <p><strong>Top Skills:</strong> <span id="profileSkills">{{user_skills}}</span></p>
        <p><strong>Area of Interest:</strong> <span id="profileInterest">{{user_interest}}</span></p>
        <p><strong>Years of Experience:</strong> <span id="profileExperience">{{user_exp}}</span></p>
        
      </div>
    </div>
    <div class="submit-button">
      <a href="/info/:id"><button>Edit</button></a>
     <a class='submit-button'  href="/"><button> Back to home </button></a>
    </div>
  </div>
</body>
</html>
